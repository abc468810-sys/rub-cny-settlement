# Database
# MySQL connection string used by Prisma (see prisma/schema.prisma).
# Format: mysql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL="mysql://user:password@localhost:3306/rub_cny_settlement"

# App (optional)
# Public base URL of the app, used for absolute links (e.g. in emails or metadata).
NEXT_PUBLIC_APP_URL="http://localhost:3000"
